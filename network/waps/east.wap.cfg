define host {
    use                     linux-server
    host_name               east.wap
    alias                   East WAP
    address                 east.wap
}

define service {
    use                     local-service
    host_name               east.wap
    service_description     PING
    check_command           check_ping!100.0,20%!500.0,60%
}
