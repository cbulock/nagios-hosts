define host {
    use                     linux-server
    host_name               west.wap
    alias                   West WAP
    address                 west.wap
}

define service {
    use                     local-service
    host_name               west.wap
    service_description     PING
    check_command           check_ping!100.0,20%!500.0,60%
}
